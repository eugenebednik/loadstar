<?xml version="1.0" encoding="utf-8"?>

<!--
  MSI packaging for Loadstar.

  Two decisions worth explaining, because both are load-bearing:

  1. SELF-CONTAINED, NOT SINGLE-FILE. The publish output is the whole .NET runtime alongside the
     app — roughly two hundred files — so someone who installs this needs nothing else, which is
     the point. Single-file would be tidier to package but extracts native libraries to a temp
     directory at startup, and this app loads WinRT projections (Windows.Graphics.Capture) plus
     System.Drawing; that is exactly the combination where extraction gets awkward. Harvesting a
     folder is the boring, reliable option.

  2. The <Files> element harvests that folder automatically. WiX v3 needed heat.exe and a generated
     fragment checked into the repo, which rots the moment a dependency changes. v4+ globs at build
     time, so a new transitive DLL is packaged without anyone remembering to re-harvest.
-->
<?define LaunchText = "Start Loadstar now" ?>

<Wix xmlns="http://wixtoolset.org/schemas/v4/wxs"
     xmlns:ui="http://wixtoolset.org/schemas/v4/wxs/ui"
     xmlns:util="http://wixtoolset.org/schemas/v4/wxs/util">

  <Package Name="Loadstar"
           Manufacturer="R3N"
           Version="$(Version)"
           UpgradeCode="6f3c0f9a-6c4e-4a3f-9a2b-7d1e5c8b4a10"
           Scope="perMachine"
           Compressed="yes"
           InstallerVersion="500">

    <!--
      Fixed for the lifetime of the product. Changing it would make future installers unable to see
      this one, so upgrades would silently install side by side instead of replacing.
    -->
    <SummaryInformation Description="Loadstar — AI game companion overlay" />

    <!-- Replaces an older install in place, and refuses to go backwards with a clear message
         rather than the default one, which reads like a bug. -->
    <MajorUpgrade DowngradeErrorMessage="A newer version of Loadstar is already installed. Uninstall it first if you want to go back to [ProductVersion]." />

    <MediaTemplate EmbedCab="yes" />

    <!-- ProgramFiles6432Folder resolves to the 64-bit Program Files on x64, which is what a
         win-x64 build belongs in. -->
    <StandardDirectory Id="ProgramFiles6432Folder">
      <Directory Id="INSTALLFOLDER" Name="Loadstar" />
    </StandardDirectory>

    <StandardDirectory Id="ProgramMenuFolder">
      <Directory Id="ShortcutFolder" Name="Loadstar" />
    </StandardDirectory>

    <Feature Id="Main" Title="Loadstar" Level="1">
      <ComponentGroupRef Id="ApplicationFiles" />
      <ComponentRef Id="StartMenuShortcut" />
    </Feature>

    <!-- Lets the user choose the install location; the alternative (WixUI_Minimal) gives them no
         say at all, which is a poor fit for something that lives in the tray permanently. -->
    <ui:WixUI Id="WixUI_InstallDir" InstallDirectory="INSTALLFOLDER" />
    <WixVariable Id="WixUILicenseRtf" Value="!(bindpath.installer)\License.rtf" />

    <!--
      Branded installer artwork, replacing WiX's stock bitmaps — the default panel is a generic disk graphic
      that makes the installer look like it belongs to some other product. Both are generated from the same
      mark as the icon; see AppIcon.RenderInstallerArt for why their text areas stay light.
    -->
    <WixVariable Id="WixUIBannerBmp" Value="!(bindpath.installer)\banner.bmp" />
    <WixVariable Id="WixUIDialogBmp" Value="!(bindpath.installer)\dialog.bmp" />

    <Property Id="ARPPRODUCTICON" Value="Loadstar.ico" />
    <Property Id="ARPURLINFOABOUT" Value="https://github.com/eugenebednik/loadstar" />

    <!--
      The .ico DIRECTLY, not extracted from the executable. Extraction was what it did before, and it worked
      only because the exe happened to carry an icon — which it did not: there was no icon resource at all, so
      every shell surface showed the generic Windows exe icon. Pointing at the icon file removes the
      indirection and the assumption behind it.

      The Id keeps the .ico extension on purpose. Windows Installer uses it to decide how to register the
      icon, and an Id without it produces an ICE warning and, on some shells, a missing icon.
    -->
    <Icon Id="Loadstar.ico" SourceFile="!(bindpath.installer)\Loadstar.ico" />

    <!--
      START THE APP WHEN SETUP FINISHES, ticked by default. Loadstar is a tray app with no window, so someone
      who installs it and sees nothing happen has no signal that it worked; the tray icon is the confirmation.

      Only offered on a fresh install, never on repair or uninstall, and only in an interactive install — a
      silent /qn install shows no ExitDialog, so nothing launches, which is the correct behaviour for a
      scripted deployment.
    -->
    <Property Id="WIXUI_EXITDIALOGOPTIONALCHECKBOXTEXT" Value="$(LaunchText)" />
    <Property Id="WIXUI_EXITDIALOGOPTIONALCHECKBOX" Value="1" />

    <!--
      WixShellExec, not a plain exe custom action, and it runs from the ExitDialog rather than the execute
      sequence. That placement is deliberate: the ExitDialog runs in the installer's CLIENT process, which is
      not elevated, so the app starts with the user's ordinary privileges. Launching it from the elevated
      server process would leave a tray app running as administrator, writing its autostart entry and settings
      into the wrong hive.
    -->
    <!--
      SetProperty rather than a plain Property, and sequenced after CostFinalize because that is when
      INSTALLFOLDER acquires a value. A literal Property containing [INSTALLFOLDER] builds with WIX1077 and
      relies on the custom action expanding it later, which works but leaves a warning that reads like a
      mistake every time anyone builds the installer.
    -->
    <SetProperty Id="WixShellExecTarget"
                 Value="[INSTALLFOLDER]Loadstar.exe"
                 After="CostFinalize"
                 Sequence="both" />

    <CustomAction Id="LaunchLoadstar"
                  BinaryRef="Wix4UtilCA_X64"
                  DllEntry="WixShellExec"
                  Impersonate="yes"
                  Return="ignore" />

    <!--
      Inside a UI element: Publish is not a child of Package. The condition is what keeps the launch to a
      fresh install, so a repair or a change does not spawn a second copy.
    -->
    <UI>
      <Publish Dialog="ExitDialog"
               Control="Finish"
               Event="DoAction"
               Value="LaunchLoadstar"
               Condition="WIXUI_EXITDIALOGOPTIONALCHECKBOX = 1 and NOT Installed" />
    </UI>

    <!--
      CLOSE THE RUNNING COPY FIRST. Loadstar lives in the tray permanently, so unlike most software it is
      running every single time somebody uninstalls or updates it — which means the files-in-use path is the
      normal path here, not an edge case. Without this, Windows Installer finds its own files locked and
      either shows the "the following applications should be closed" dialog or defers the deletions to a
      reboot, leaving a half-removed install behind until the machine restarts.

      RebootPrompt="no" is the load-bearing attribute: it is what stops a successful uninstall from ending
      in a restart request.

      TerminateProcess after CloseMessage, because asking is not enough on its own. CloseMessage sends
      WM_CLOSE to top-level windows, and a tray app's only windows are a hidden message sink and a
      NotifyIcon — neither obliged to respond. Terminating loses nothing: settings are written when the
      dialog is accepted, not at exit, so there is no unsaved state to lose. Prompting is disabled for the
      same reason a silent uninstall should stay silent.
    -->
    <util:CloseApplication Id="CloseLoadstar"
                           Target="Loadstar.exe"
                           Description="Loadstar is running and must be closed before it can be removed."
                           CloseMessage="yes"
                           TerminateProcess="0"
                           RebootPrompt="no"
                           PromptToContinue="no" />

    <!--
      Hands the per-user leftovers back to the app, which is the only thing that can reach them. See
      UninstallCleanup for what it removes and why the package cannot do it directly.

      DEFERRED, and that is the whole trick. The rule is the opposite of what it sounds like:

        - Execute="immediate" runs in whichever process is walking the sequence. For a perMachine package
          that is the SERVER process, which is LocalSystem. An earlier version of this action was immediate
          with Impersonate="yes", which reads like it would run as the user and does not — verified against
          a real uninstall log, where it appeared as `MSI (s) ... RemoveUserData ... Return value 1`. It
          "succeeded" against SYSTEM's own registry hive and SYSTEM's own AppData, so it deleted nothing and
          reported success, and the user's autostart entry and API key were both still there afterwards.

        - Execute="deferred" WITHOUT NoImpersonate impersonates the user who started the install. That is
          the documented behaviour and the only way to reach their HKCU and their profile.

      So deferred is not about elevation here — it is the only route to the correct user context.

      THE COMMAND LINE ARRIVES IN A PROPERTY, which is the second thing this action got wrong before. A
      deferred action cannot resolve properties: formatted fields like [INSTALLFOLDER] are never expanded for
      one, only CustomActionData is. So both obvious spellings fail —

        - putting [INSTALLFOLDER] straight in the command line passes the literal brackets through; and
        - naming the executable relatively, with Directory="INSTALLFOLDER", fails with error 1721. MSI does
          resolve the directory (the log showed Source=C:\Program Files\Loadstar\) but CreateProcess looks a
          bare executable name up against the CALLING process's directory, not the working directory it was
          handed, so nothing is found. Return="ignore" then swallows it and the uninstall reports success.

      SetProperty below runs as an ordinary immediate action, where [INSTALLFOLDER] IS expanded, and stores
      the result in a property named the same as this action. MSI resolves that property when it writes the
      deferred action into the script, so what lands there is a full absolute path.

      THE PROPERTY HOLDS THE EXECUTABLE ONLY, and the arguments stay in ExeCommand. MSI joins the two itself.
      Putting the whole command line in the property also fails with 1721, and the log says precisely why:
      it logged the whole string as the `location` and left `command` empty, so it looked for a program whose
      filename contained the flag.

      Unquoted, despite the space in "Program Files": this field is a path, not a command line, and MSI quotes
      it when building the process. A quoted value becomes part of the filename it searches for.

      Return="ignore" so a failure to tidy up can never fail an uninstall.
    -->
    <CustomAction Id="RemoveUserData"
                  Property="RemoveUserData"
                  ExeCommand="--uninstall-cleanup"
                  Execute="deferred"
                  Impersonate="yes"
                  Return="ignore" />

    <!-- Same condition as the action it feeds: no point building a command line for something that will
         not run, and SetProperty on an uninstall-only path should not fire during an upgrade either. -->
    <SetProperty Id="RemoveUserData"
                 Value="[INSTALLFOLDER]Loadstar.exe"
                 Before="RemoveUserData"
                 Sequence="execute"
                 Condition="REMOVE=&quot;ALL&quot; AND NOT UPGRADINGPRODUCTCODE" />

    <InstallExecuteSequence>
      <!--
        Before RemoveFiles, because the executable being invoked is one of the files about to be deleted.
        A deferred action is written into the installation script at its sequence position, so scheduling it
        here puts it ahead of RemoveFiles' own operations when the script executes, not merely ahead of them
        during the planning pass.

        The condition is what keeps an update from wiping settings. REMOVE="ALL" alone is true during a
        MajorUpgrade as well, since that works by uninstalling the old package — so on every version bump
        this would delete the user's API key and configuration and look like the app losing them for no
        reason. UPGRADINGPRODUCTCODE is set only in that case, so excluding it narrows this to a genuine
        uninstall.
      -->
      <Custom Action="RemoveUserData"
              Before="RemoveFiles"
              Condition="REMOVE=&quot;ALL&quot; AND NOT UPGRADINGPRODUCTCODE" />
    </InstallExecuteSequence>

  </Package>

  <Fragment>
    <!--
      PublishDir comes in as a bind path so this file has no knowledge of where the build put
      things. Everything under it is packaged, which is the whole runtime plus the app.
    -->
    <ComponentGroup Id="ApplicationFiles" Directory="INSTALLFOLDER">
      <Files Include="!(bindpath.publish)\**" />
    </ComponentGroup>
  </Fragment>

  <Fragment>
    <!--
      A shortcut component needs a KeyPath, and a registry value is the conventional choice because
      the shortcut itself cannot be one. HKLM rather than HKCU: this is a perMachine install, and an
      HKCU keypath in a perMachine package is the classic cause of ICE38 and of repair loops when a
      second user logs in.
    -->
    <Component Id="StartMenuShortcut" Directory="ShortcutFolder" Guid="1c9a7e42-3b58-4d6f-8e21-9f4c7a0d5b83">
      <Shortcut Id="LoadstarStartMenu"
                Name="Loadstar"
                Description="AI game companion overlay"
                Target="[INSTALLFOLDER]Loadstar.exe"
                WorkingDirectory="INSTALLFOLDER"
                Icon="Loadstar.ico" />

      <!--
        An explicit way out, in the same folder as the app. Settings → Installed apps already offers one and
        is the modern route, but this app spends its life as a tray icon with no window and no menu bar, so a
        user who wants rid of it has nothing on screen to work from and has to know where Windows keeps that
        list. The Start Menu entry is where they will look first.

        msiexec /x [ProductCode] rather than a path to anything: ProductCode is substituted at build time and
        the command keeps working regardless of where the package was installed from or whether it still
        exists. System64Folder because the package is x64.
      -->
      <Shortcut Id="UninstallStartMenu"
                Name="Uninstall Loadstar"
                Description="Removes Loadstar from this computer"
                Target="[System64Folder]msiexec.exe"
                Arguments="/x [ProductCode]" />

      <RemoveFolder Id="RemoveShortcutFolder" Directory="ShortcutFolder" On="uninstall" />

      <RegistryValue Root="HKLM"
                     Key="Software\Loadstar"
                     Name="Installed"
                     Type="integer"
                     Value="1"
                     KeyPath="yes" />
    </Component>
  </Fragment>

</Wix>
